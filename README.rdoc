= DESCRIPTION:
Sets up a basic xinetd configuration.

= REQUIREMENTS:
Ubuntu 12.04 LTS

= ATTRIBUTES:

= USAGE:
Important! Call 'include_recipe xinetd' before calling the provider!

Example:
xinetd_service "test" do
  app_name "test"
  port "6977"
  server '/bin/echo Testing'
  action :add
end
